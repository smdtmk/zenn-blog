---
title: "VPCフローログ確認手順"
emoji: "🌊"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["aws", "vpc", "athena", "s3"]
published: false
---

# VPCフローログをAmazon Athenaで調査する方法

VPCフローログをAmazon Athenaで分析する方法について解説します。VPC内のネットワークトラフィックを可視化し、セキュリティ問題やネットワークトラブルの調査に役立てましょう。

## VPCフローログとは

VPCフローログは、VPC内のネットワークインターフェイス間で送受信されるIPトラフィックに関する情報をキャプチャする機能です。

### フローログを設定できるリソース

- VPC
- サブネット
- ネットワークインターフェイス（ENI）

どのリソースに設定しても、実際にトラフィックをキャプチャするのはネットワークインターフェイス（ENI）です。VPCやサブネットに設定した場合は、その配下のすべてのENIが対象となります。

### 出力先

- **CloudWatch Logs**: リアルタイム監視やアラーム発報に適している
- **S3バケット**: 長期保存やAthenaでの分析に適している

本記事では、S3バケットに出力してAthenaで分析する方法を解説します。

## 前提条件

- VPCフローログがS3バケットに出力されるように設定済みであること
- Athenaでクエリを実行する権限があること

## 設定手順

### 1. VPCフローログをS3へ出力するように設定

まず、VPCフローログの出力先となるS3バケットを作成します。

次に、VPCコンソールで対象VPCの「フローログ」タブから「フローログの作成」をクリックし、以下の設定を行います：

![VPCフローログ作成画面](./images/vpcflowlog-check-manual/vpc-flow-log-creation.png)

- **フィルタ**: 「すべて」（承諾・却下の両方を記録）
- **送信先**: 「S3バケットへの送信」
- **S3バケットARN**: 送信先バケットのARNを指定（例: `arn:aws:s3:::bucket-name`）
- **Log record format**: デフォルトまたはカスタム形式を選択

フローログを作成すると、指定したS3バケットに対して自動的にバケットポリシーが設定されます。通常はこの自動設定で問題ありませんが、アカウントまたぎなど特殊な要件がある場合はポリシーを修正してください。

### 2. S3バケットにフローログが記録されることを確認

フローログを作成して数分〜数十分経つと、S3バケット内にログが保存され始めます。

![S3バケット内のログファイル構造](./images/vpcflowlog-check-manual/s3-log-structure.png)

ログの保存先は以下のフォルダ階層となります：

```
{保存先バケット}/AWSLogs/{アカウントID}/vpcflowlogs/{リージョン名}/YYYY/MM/DD/
```

ログファイル名の命名規則：

```
{アカウントID}_vpcflowlogs_{リージョン名}_{フローログID}_{タイムスタンプ}_{ハッシュ値}.log.gz
```

**注意**: ネットワークインターフェイスにトラフィックが発生しない限り、ログは保存されません。

### 3. Athenaでデータベースを作成

Athenaコンソールの「クエリエディタ」を開き、以下のクエリを実行してデータベースを作成します：

```sql
CREATE DATABASE VPCFlowLogs;
```

![Athenaでデータベースを作成](./images/vpcflowlog-check-manual/athena-create-database.png)

クエリが成功したら、左上の「Database」プルダウンから作成したデータベースを選択します。

### 4. Athenaで外部テーブルを作成

S3バケットに保存されたフローログをAthenaで分析できるように、外部テーブルを作成します。

```sql
CREATE EXTERNAL TABLE IF NOT EXISTS vpc_flow_logs (
  version int,
  account string,
  interfaceid string,
  sourceaddress string,
  destinationaddress string,
  sourceport int,
  destinationport int,
  protocol int,
  numpackets int,
  numbytes bigint,
  starttime int,
  endtime int,
  action string,
  logstatus string
)
PARTITIONED BY (dt string)
ROW FORMAT DELIMITED
FIELDS TERMINATED BY ' '
LOCATION 's3://your-bucket-name/AWSLogs/123456789012/vpcflowlogs/ap-northeast-1/'
TBLPROPERTIES ("skip.header.line.count"="1");
```

![Athenaでテーブルを作成](./images/vpcflowlog-check-manual/athena-create-table.png)

**ポイント**:

- `LOCATION`句のS3パスは、日付部分（`YYYY/MM/DD/`）を含めません
- S3バケット名、アカウントID、リージョン名は実際の値に置き換えてください
- テーブルの列名は、VPCフローログのフィールド名と対応させます（ハイフンはアンダースコアに置き換え）

### 5. パーティションを追加してデータを取り込む

テーブルを作成しただけでは、まだデータは取り込まれていません。パーティションを追加することで、対象のデータをテーブルにロードします。

特定の日付のパーティションを追加する例：

```sql
ALTER TABLE vpc_flow_logs
ADD PARTITION (dt='2025-02-23')
location 's3://your-bucket-name/AWSLogs/123456789012/vpcflowlogs/ap-northeast-1/2025/02/23';
```

複数の日付分をまとめて追加する場合は、以下のようなスクリプトを使用できます：

```bash
#!/bin/sh
START_DATE=2025-02-01
END_DATE=2025-02-28
TABLE_NAME=vpc_flow_logs
LOG_PATH=s3://your-bucket-name/AWSLogs/123456789012/vpcflowlogs/ap-northeast-1

CUR_DATE=$START_DATE
echo "ALTER TABLE $TABLE_NAME ADD"
while :
do
  SUB_DIR=`date -d "$CUR_DATE" "+%Y/%m/%d"`
  echo -n "PARTITION (dt="\'"$CUR_DATE"\'") location "\'"$LOG_PATH/$SUB_DIR/"\'
  if [ $CUR_DATE = $END_DATE ]; then
    echo ";"
    break
  else
    echo
  fi
  CUR_DATE=`date -d "$CUR_DATE 1 day" "+%Y-%m-%d"`
done
```

**注意**: パーティションの追加は、まだ存在しないS3パスを指定しても可能です。将来分のパーティションを事前に追加しておくことで、後からデータが保存された際に自動的にクエリ可能になります。

## クエリ例

### 例1: 期間を指定してログを抽出

特定の時間帯のログを抽出します：

```sql
SELECT
  from_unixtime(starttime, 9, 0) AS starttime_jst,
  from_unixtime(endtime, 9, 0) AS endtime_jst,
  interfaceid,
  sourceaddress,
  destinationaddress,
  sourceport,
  destinationport,
  protocol,
  numpackets,
  numbytes,
  action,
  logstatus
FROM vpc_flow_logs
WHERE from_unixtime(starttime, 9, 0) >= cast('2025-02-23 12:00:00 +09:00' as timestamp with time zone)
  AND from_unixtime(starttime, 9, 0) <  cast('2025-02-23 13:00:00 +09:00' as timestamp with time zone)
ORDER BY starttime;
```

![クエリ結果の例（期間指定）](./images/vpcflowlog-check-manual/query-result-time-range.png)

### 例2: 拒否されたパケットのアクセス元IPアドレスの上位10個

セキュリティ調査に有用なクエリです：

```sql
SELECT sourceaddress, COUNT(*) AS num_of_access
FROM vpc_flow_logs
WHERE action='REJECT'
GROUP BY sourceaddress
ORDER BY num_of_access DESC
LIMIT 10;
```

![クエリ結果の例（拒否パケットの集計）](./images/vpcflowlog-check-manual/query-result-reject-summary.png)

### 例3: 特定のポートへのアクセスを調査

```sql
SELECT
  from_unixtime(starttime, 9, 0) AS starttime_jst,
  sourceaddress,
  destinationaddress,
  destinationport,
  action,
  numpackets,
  numbytes
FROM vpc_flow_logs
WHERE destinationport = 22  -- SSH
  AND dt >= '2025-02-23'
ORDER BY starttime DESC
LIMIT 100;
```

### 例4: プロトコル別のトラフィック量を集計

```sql
SELECT
  CASE protocol
    WHEN 6 THEN 'TCP'
    WHEN 17 THEN 'UDP'
    WHEN 1 THEN 'ICMP'
    ELSE CAST(protocol AS VARCHAR)
  END AS protocol_name,
  SUM(numpackets) AS total_packets,
  SUM(numbytes) AS total_bytes,
  COUNT(*) AS flow_count
FROM vpc_flow_logs
WHERE dt >= '2025-02-23'
GROUP BY protocol
ORDER BY total_bytes DESC;
```

## 注意事項

### 列名について

- VPCフローログのフィールド名のハイフン（`-`）は、Athenaの列名ではアンダースコア（`_`）に置き換えます
- Athenaの予約キーワードを使用する場合は、バックティック（`` ` ``）で囲みます

### パーティション分割の重要性

パーティション分割により、以下のメリットがあります：

- **クエリ速度の向上**: 必要な日付のデータのみをスキャンするため、高速化されます
- **コスト削減**: スキャンするデータ量が減るため、Athenaの課金も削減されます

WHERE句で`dt`列を使用してパーティションを指定することで、効率的にクエリできます：

```sql
SELECT *
FROM vpc_flow_logs
WHERE dt >= '2025-02-23'
  AND dt <= '2025-02-25'
  AND action = 'REJECT';
```

## まとめ

VPCフローログをS3バケットに出力し、Amazon Athenaで分析する方法について解説しました。

- VPCフローログをS3に出力する設定
- Athenaでデータベースとテーブルを作成
- パーティションを追加してデータを取り込む
- 様々なクエリでログを分析

セキュリティ問題やネットワークトラブルの調査時に、大量のVPCフローログから必要な情報を効率的に抽出できるようになります。実際に設定して、必要な時にスムーズにAthenaが使えるようにしておくと良いでしょう。

## 参考資料

- [Amazon VPC フローログをクエリする - Amazon Athena](https://docs.aws.amazon.com/ja_jp/athena/latest/ug/vpc-flow-logs.html)
- [VPCフローログをAmazon Athenaで分析する | DevelopersIO](https://dev.classmethod.jp/articles/vpc-flow-logs-athena/)
